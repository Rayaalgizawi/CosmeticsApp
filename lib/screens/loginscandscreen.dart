import 'package:flutter/material.dart';

class Loginscandscreen extends StatelessWidget {
  const Loginscandscreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Loginscandscreen is working',
        style: TextStyle(fontSize: 20),
      ),
    );
  }
}
